{% extends 'base.html.twig' %}

{% block body %}
    <h1>Filiere</h1>

    <table>
        <tbody>
            <tr>
                <th>Designation</th>
                <td>{{ filiere.designation }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ filiere.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('filieres_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('filieres_edit', { 'id': filiere.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
